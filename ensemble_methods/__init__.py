"""
ensemble_methods package
------------------------
URL scam detection using ensemble learning — implemented from scratch
(no scikit-learn, no numpy).

Modules
-------
features   -- URL → numeric feature vector
data       -- CSV loading and train/test splitting
models     -- DecisionTree, RandomForest, AdaBoost, StackingEnsemble, LogisticRegression
evaluate   -- accuracy, precision/recall/F1, confusion matrix, report printer
main       -- CLI entry point tying everything together
"""
